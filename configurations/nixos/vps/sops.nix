# sops configuration deferred - set up after initial bootstrap
# For now, disable sops to allow initial installation
{
  # sops.secrets will be configured after VPS is running
  # See VPS_SETUP.md for sops configuration steps
}
